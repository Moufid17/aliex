{% extends 'base.html.twig' %}

{% block title %}{{app.user.username}} - Addresses | Aliex{% endblock %}

{% block content %}
<div class="container-fluid">
    <h2> Gérer mes adresses </h2>
    <hr>
    {{ form_start(form) }}
    <div class="row">
        <div class="col-6">{{ form_row(form.name) }}</div>
        <div class="col-6">{{ form_row(form.street) }}</div>
    </div>
    <div class="row">
        <div class="col-4">{{ form_row(form.city) }}</div>
        <div class="col-4">{{ form_row(form.codepostal) }}</div>
        <div class="col-4">{{ form_row(form.country) }}</div>
    </div>
    <div class="row">
        <div class="col-6">{{ form_row(form.phone) }}</div>
        <div class="col-6">{{ form_row(form.company) }}</div>
    </div>
    <button class="btn btn-sm btn-dark w-25 d-auto my-3" width="100%" type="submit">
        Valider
    </button>
    {{ form_end(form) }}
</div>
{% endblock %}
